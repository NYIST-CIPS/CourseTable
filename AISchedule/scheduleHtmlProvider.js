function scheduleHtmlProvider(iframeContent = "", frameContent = "", dom = document) {
    alert("导入步骤（重要！！！）：👉1.统一认证登录（默认账号：学号；默认密码：身份证后6位）👉2.点击右下角导入")
    return dom.getElementsByClassName('course-wrap')[0].innerHTML;
}